1. computers are tools
2. the shell is powerful
3. basic shell commands:
    pwd
    cd
    ls
4. file editing:
    cp = copy file
    mv = move/rename
    rmdir = removed empty directory
    rm = remove file
    command -r = recursively (including directories)
    
